{% extends 'partials/base.html.twig' %}

{% block article %}
    {% include 'partials/no-hero-header.html.twig' with {
        'title': page.header.title,
        'links': [
            {
                'url': '/',
                'title': "BSCOPE.LOGIN_HEADER_HOME"|t

            },
            {
                'title': page.header.title|t

            }
        ]
    } %}

    <div class="container">
        <div class="contact-inner">
            <div class="email-section">
                {# <div class="text-content">

                    <h3 class="text-brand">{{ 'METRICALO.SEND_AN_EMAIL'|t }}</h3>
                    <p>Our friendly team would love to hear from you.</p>
                </div> #}
                {% include 'forms/form.html.twig' with {form: forms('contact-us'), action: '#'} %}
            </div>
            <div class="contact-section">
                <div class="text-content">
                    <h3  class="text-brand">
                        Have any questions?
                    </h3>
                    <p class="message">Get in touch!</p>                
                </div>
                {# <div class="text-content">

                    <h3 class="text-brand">{{ 'METRICALO.SEND_AN_EMAIL'|t }}</h3>
                    <p>Our friendly team would love to hear from you.</p>
                </div> #}
                <p><img src="{{ url('theme://images/email.svg') }}" /> <a href="mailto:{{ webdata('support_email') }}" target="_blank" rel="noopener noreferrer"><span class="span">{{ webdata('support_email') }}<br /></span></a></p>
                
                <p><img src="{{ url('theme://images/phone.svg') }}" /> <a href="tel:{{ webdata('support_phone') }}" target="_blank" rel="noopener noreferrer"><span class="span">{{ webdata('support_phone') }}<br /></span></a></p>
                <p><img src="{{ url('theme://images/work.svg') }}" /> <span class="span">{{ webdata('support_hours') }}<br /></span></p>
                <p><img src="{{ url('theme://images/location.svg') }}" /> <span class="span"> {{ webdata('company.address') }}</span></p>
                {# <p><img src="" /> <span class="span"> {{ webdata('company.business_number') }}</span></p> #}
                {# <p><img src="" /> <span class="span"> {{ webdata('company.vat_number') }}</span></p> #}
            </div>
        </div>
    </div>

{% endblock %}
